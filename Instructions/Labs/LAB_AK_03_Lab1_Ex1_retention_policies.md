---
lab:
  title: 연습 1 - 보존 정책 구성
  module: Module 3 - Implement Data Lifecycle and Records Management
---

## WWL 테넌트 - 사용 약관

강사 주도 교육 제공의 일부로 테넌트를 제공하는 경우, 강사 주도 교육에서 실습 랩을 지원하기 위해 테넌트를 사용할 수 있습니다.

테넌트를 실습 랩 외부에서 공유하거나 사용해서는 안 됩니다. 이 과정에서 사용되는 테넌트는 평가판 테넌트이며 클래스가 종료된 후 사용하거나 액세스할 수 없으며 확장판에서도 사용할 수 없습니다.

테넌트를 유료 구독으로 변환해서는 안 됩니다. 이 과정의 일부로 얻은 테넌트는 Microsoft Corporation의 재산으로 유지되며 언제든지 액세스 권한을 획득하고 다시 소유할 수 있는 권리를 보유합니다.

# 랩 3 - 연습 1 - 보존 정책 구성

이 연습에서는 Contoso Ltd의 시스템 관리 주체인 Joni Sherman의 역할을 맡게 됩니다. 귀하의 조직은 텍사스에 본사를 두고 있으며, 범죄를 구성하지 않고 3 년 후에 기록을 삭제 할 수 있다고 규정하는 주법을 준수하기 위해 보존 정책을 구현하려고합니다.

이 법을 준수하기 위해 조직은 3년 동안 조직의 모든 항목을 보존하는 보존 계획을 만들었습니다.

## 작업 1 – 회사 전체 보존 정책 만들기

이 연습에서는 회사 전체 보존 정책을 만들고, 보존 기간을 적용하고, 정책이 적용될 위치를 설정합니다.

1. 클라이언트 1 VM(LON-CL1)에 lon-cl1\admin** 계정으로 **로그인합니다.

1. **Microsoft Edge**에서 **https://compliance.microsoft.com**으로 이동한 다음, Microsoft Purview 포털에 **Joni Sherman** JoniS@WWLxZZZZZZ.onmicrosoft.com으로 로그인합니다(ZZZZZZ는 랩 호스팅 공급자가 제공한 고유 테넌트 ID임).  Joni의 암호는 랩 호스팅 공급자가 제공해야 합니다.

1. Microsoft Purview 포털의 **왼쪽 탐색 창에서 데이터 수명 주기 관리를** 확장**한 다음, Microsoft 365**를 선택합니다**.**

1. **데이터 수명 주기 관리** 페이지의 보존 정책** 탭에서 **+ 새 보존 정책을** 선택합니다**.

1. **보존 정책** 이름 페이지의 이름** 및 **설명**에 **대해 다음 정보를 입력합니다.

    - **이름**: 회사 전체
    - **설명**: 팀을 제외한 모든 위치

1. **다음** 버튼을 선택합니다.  

1. **정책 범위** 페이지에서 다음**을 선택합니다**.

1. **영역을 만들** 보존 정책 유형 선택에서 정적**을 선택한 **다음, 다음**을 선택합니다**.

1. **이 정책을** 적용할 위치 선택에서 다음을 사용하도록 설정합니다.

   - **Exchange 사서함**
   - **SharePoint 클래식 및 통신 사이트**
   - **OneDrive 계정**
   - **Microsoft 365 그룹 사서함 및 사이트**

1. **다음**을 선택합니다.

1. **특정 기간**의 항목 보존 섹션에 대해 **콘텐츠를 보존할지, 삭제할지 또는 두 페이지를 모두** 보존할지 결정 섹션에서 다음 정보를 입력합니다.

   - **특정 기간** 동안 항목 보존: 드롭다운 목록에서 사용자 지정** 선택 **
   - 연도 필드를 **3**으로 변경합니다.
   - **항목이 마지막으로 수정된 시간 기준** 보존 기간 시작
   - **보존 기간이 끝날 때**: 자동으로 항목 삭제

1. **다음**을 선택합니다.

1. **검토 및 완료** 페이지에서 제출**을 선택합니다**.

1. 정책이 만들어지면 완료**를 선택합니다**.

Exchange 전자 메일, Microsoft 365 그룹, OneDrive 및 SharePoint 사이트 위치에 대한 보존 정책을 성공적으로 만들었습니다. 이 보존 정책은 항목이 마지막으로 수정된 날짜로부터 3년 동안 이러한 위치에 항목을 유지합니다. 이 정책은 테넌트에서 적용하는 데 최대 24시간이 걸릴 수 있지만 다음 단계를 진행할 수 있습니다.

## 작업 2 – 필터를 사용하여 위치 기반 보존 정책 만들기

이제 Teams 위치에 대한 보존 정책을 만듭니다. Teams 채널에 문서가 포함될 수 있으므로 모두 보존됩니다. 조직에서는 제한된 수의 사용자가 팀 채팅에 보존 기간이 필요하다고 결정했습니다.

1. 여전히 클라이언트 1 VM(LON-CL1)에 lon-cl1\admin 계정으로 **로그인해야 하며, Joni Sherman**으로 **Microsoft 365에 로그인해야** 합니다.

1. **Microsoft Edge**에는 Microsoft Purview 포털 탭이 계속 열려 있어야 합니다. 그렇다면 선택하고 다음 단계로 진행합니다. 닫았다면 새 탭에서 **https://compliance.microsoft.com**으로 이동합니다.

1. Microsoft Purview 포털의 **왼쪽 탐색 창에서 데이터 수명 주기 관리를** 확장**한 다음, Microsoft 365**를 선택합니다**.**

1. **데이터 수명 주기 관리** 페이지의 **보존 정책** 탭에서 **+ 새 보존 정책**을 선택합니다.

1. **보존 정책** 이름 페이지의 이름** 및 **설명**에 **대해 다음 정보를 입력합니다.

   - **이름**: Teams 보존
   - **설명**: Teams 위치에 대한 보존

1. **다음**을 선택합니다.

1. **정책 범위** 페이지에서 다음**을 선택합니다**.

1. **영역을 만들** 보존 정책 유형 선택에서 정적**을 선택한 **다음, 다음**을 선택합니다**.

1. **정책** 페이지를 적용할 위치 선택에서 다음을 사용하도록 설정합니다.

   - **Teams 채널 메시지**
   - **Teams 채팅**
   - 다른 모든 위치를 사용하지 않도록 설정합니다.

1. **Teams 채팅** 위치에 대해 **모든 사용자** 아래에 있는 **편집** 텍스트 링크를 선택합니다.

1. **Teams 채팅 플라이아웃 페이지에서 사용자를 추가합니다**.

    - Adele Vance
    - Pradeep Gupta

    >**참고**: 이 두 사용자를 추가하면 Teams 채팅에 ****포함된** 설정이** 모든 팀에서* 2명의 사용자*로 *변경*됩니다.

1. 완료를 선택한 **다음, 다음**을 선택합니다**.**

1. **특정 기간**의 항목 보존 섹션에 대해 **콘텐츠를 보존할지, 삭제할지 또는 두 페이지를 모두** 보존할지 결정 섹션에서 다음 정보를 입력합니다.

   - **특정 기간** 동안 항목 보존: 드롭다운 목록에서 사용자 지정** 선택 **
   - 연도 필드를 **3**으로 변경합니다.
   - **항목이 마지막으로 수정된 시간 기준** 보존 기간 시작
   - **보존 기간이 끝날 때**: 자동으로 항목 삭제

1. **다음**을 선택합니다.

1. **검토 및 완료** 페이지에서 제출**을 선택합니다**.

1. **보존 정책** 페이지를 성공적으로 만들었으면 완료**를 선택합니다**.

Teams 위치에 대한 보존 정책을 성공적으로 만들었습니다. 모든 Teams 채널 위치에 대해 3년의 보존 기간을 설정합니다. 특정 사용자에게만 적용되도록 Teams 채팅 위치에 대한 필터를 설정했습니다.

## 작업 3 – PowerShell을 통해 보존 정책 만들기

PowerShell을 사용하여 동일한 보존 정책을 만듭니다.

1. 클라이언트 1 VM(LON-CL1)에 lon-cl1\admin** 계정으로 **로그인합니다.

1. 마우스 오른쪽 단추가 있는 Windows 단추를 선택하여 관리자 권한 PowerShell 창을 연 다음, 사용자 계정 컨트롤 창과 마주치면 Windows PowerShell(관리)**을 선택합니다******.

1. 다음 cmdlet을 사용하여 테넌트에서 보안 및 규정 준수 센터에 커넥트.

    ```powershell
    Connect-IPPSSession
    ```

1. 로그인 대화 상자가 표시되면 **MOD 관리자**로 로그인합니다. 로그인 ID로는 admin@WWLxZZZZZZ.onmicrosoft.com을 사용합니다(여기서 ZZZZZZ는 랩 호스팅 공급자가 제공한 고유 테넌트 ID임).  관리 암호는 랩 호스팅 공급자가 제공해야 합니다.

1. 다음 cmdlet을 실행하여 팀을 제외한 모든 위치에 대한 첫 번째 보존 정책을 만듭니다.

    ```powershell
    New-RetentionCompliancePolicy -Name "Company Wide PS" -ExchangeLocation All -ModernGroupLocation All -SharePointLocation All -OneDriveLocation All
    ```

1. 다음 cmdlet을 실행하여 보존 기간을 설정합니다. 단위로는 수정한 날짜 기준 기간(일)을 사용합니다.

    ```powershell
    New-RetentionComplianceRule -Name "Company Wide PS Rule" -Policy "Company Wide PS" -RetentionDuration 1095 -ExpirationDateOption ModificationAgeInDays -RetentionComplianceAction Keep
    ```

1. 다음 cmdlet을 실행하여 Teams 위치에 대한 두 번째 보존 정책을 만듭니다.

    ```powershell
    New-RetentionCompliancePolicy -Name "Teams Retention PS" -TeamsChannelLocation All -TeamsChatLocation "Adele Vance", "Pradeep Gupta"
    ```

1. 다음 cmdlet을 실행하여 보존 기간을 단위로 사용하여 설정합니다.

    ```powershell
    New-RetentionComplianceRule -Name "Teams Retention PS Rule" -Policy "Teams Retention PS" -RetentionDuration 1095 -RetentionComplianceAction Keep
    ```

보존 기간이 3년인 PowerShell을 통해 보존 정책을 성공적으로 만들었습니다.

## 작업 4 – 적응형 범위를 사용하여 보존 정책 만들기

이 연습에서는 재무 및 법률 부서에 대한 보존 정책을 만듭니다. 이 정책의 목적은 법률을 준수하여 5년 동안 모든 법적 관련 문서를 보존하는 것입니다. 먼저 법률 및 소매 부서를 포함한 적응형 범위를 만든 다음, 이 범위를 사용하여 보존 정책을 만듭니다.

1. 여전히 클라이언트 1 VM(LON-CL1)에 lon-cl1\admin 계정으로 **로그인해야 하며, Joni Sherman**으로 **Microsoft 365에 로그인해야** 합니다.

1. **Microsoft Edge**에는 Microsoft Purview 포털 탭이 계속 열려 있어야 합니다. 그렇다면 선택하고 다음 단계로 진행합니다. 닫았다면 새 탭에서 **https://compliance.microsoft.com**으로 이동합니다.

1. 왼쪽 탐색 창의 **Microsoft Purview** 포털에서 역할 및 범위를 확장**한** 다음, 적응형 범위를** 선택합니다**.

1. **적응형 범위** 페이지에서 + 범위** 만들기를 선택합니다**.

1. 이름에 **적응형 정책 범위** 페이지 입력:

    - **이름**: 법적 문서 보존
    - **설명**: 법적 관련 문서에 대한 보존

1. **다음**을 선택합니다.

1. **관리 단위** 할당 페이지에서 다음**을 선택합니다**.

1. **만들려는 범위 유형은 무엇인가요?** 페이지에서 사용자를** 선택한 **다음, 다음**을 선택합니다**.

1. 사용자 정의 쿼리 만들기 페이지의 사용자 특성** 아래에서 **특성**에 대한 **드롭다운 메뉴를 선택한 다음, 부서**를 선택합니다**.** ** 

1. 특성 필드 선택 **바로 옆에 있는 연** 산자로 동일합니다.

1. 값** 필드의 입력 **법적** 값**입니다.

1. 두 번째 특성을 추가하려면 **사용자 정의 쿼리 만들기** 페이지에서 **+ 특성 추가**를 선택합니다.

1. 쿼리 연산자 **, 특성 **, **** 연산자** 및 **값** 입력의** 경우:

   - **쿼리 연산자**: Or
   - **특성**: 부서
   - **연산자**: 다음과 같음
   - **값**: 소매

1. 각 특성 옆에 검사box가 선택되어 있는지 확인하고 다음**을 선택합니다**.

1. **검토 및 완료** 페이지에서 제출**을 선택합니다**.

1. 범위를 **만든 페이지에서** 완료**를 선택합니다**.

1. Microsoft Purview 포털의 **왼쪽 탐색 창에서 데이터 수명 주기 관리를** 확장**한 다음, Microsoft 365**를 선택합니다**.**

1. **데이터 수명 주기 관리** 페이지에서 보존 정책** 탭을 **선택한 다음+ 새 보존 정책을** 선택합니다**.

1. 이름에 **보존 정책** 페이지 입력:

    - **이름**: 법적 데이터 보존
    - **설명**: 법률 및 소매 부서 내의 모든 문서를 보존합니다.

1. **다음**을 선택합니다.

1. **정책 범위** 페이지에서 다음**을 선택합니다**.

1. **보존 정책 유형을 선택하여 만들** 페이지에서 적응형**을 선택한 **다음, 다음**을 선택합니다**.

1. **적응형 정책 범위 및 위치** 선택 페이지에서 + 범위 추가를** 선택합니다**.

1. 오른쪽 플라이아웃 **적응형 정책 범위** 선택 페이지에서 검사사용 문서 보존** 상자를 **선택한 다음 추가** 단추를 선택합니다**.

1. **정책을** 적용할 위치 선택으로 돌아가서 다음을 사용하도록 설정합니다.

    - **Exchange 사서함**
    - **OneDrive 계정**
    - 다른 모든 위치를 사용하지 않도록 설정합니다.

1. **다음**을 선택합니다.

1. **특정 기간** 섹션의 보존 항목에 대한 **콘텐츠를 보존할지, 삭제할지 또는 두 페이지 모두**에 대해 결정에서 다음을 입력합니다.

    - **특정 기간 동안 항목 보존**: 5년
    - **다음을 기반으로 보존 기간을 시작합니다**. 항목이 만들어진 경우
    - **보존 기간이** 끝나면 다음을 수행합니다.

1. **다음**을 선택합니다.

1. **검토 및 완료** 페이지에서 제출**을 선택합니다**.

1. 정책이 만들어지면 완료** 단추를 선택합니다**.

1. **보존 정책** 페이지를 성공적으로 만들었으면 완료**를 선택합니다**.

보존 정책에 적응형 범위를 적용했습니다.

## 작업 5 – 적응형 범위 정책 테스트

이 연습에서는 적응형 범위의 영향을 받는 사용자를 확인하고 새 적응형 보존 정책을 테스트합니다.

>**참고**: 보존 정책을 만들고 제출할 때 보존 정책을 적용하는 데 최대 7일이 걸릴 수 있습니다.

1. lon-cl1\admin**에 **로그인한 적응 범위 보존 정책의 세부 정보를 검토하려면 마우스 오른쪽 단추가 있는 Windows 단추를 선택하여 PowerShell 창을 연 다음 Windows PowerShell을 선택합니다.

1. 다음 cmdlet을 사용하여 테넌트에서 보안 및 규정 준수 센터에 커넥트.

    ```powershell
    Connect-IPPSSession
    ```

1. 로그인 대화 상자가 표시되면 Joni Sherman의 계정  JoniS@WWLxZZZZZZ.onmicrosoft.com 으로 로그인합니다(여기서 ZZZZZZ는 랩 호스팅 공급자가 제공하는 고유한 테넌트 ID임). 관리 암호는 랩 호스팅 공급자가 제공해야 합니다.

1. 적응형 범위 정책의 모든 세부 정보를 보려면 다음 cmdlet을 실행합니다.

    ```powershell
    Get-RetentionCompliancePolicy -Identity "Legal Data Retention"     -DistributionDetail | Format-List
    ```

1. 세부 정보를 검토합니다. 특정 매개 변수에는 다음과 같은 상태 있어야 합니다.

    - **Enabled**: True
    - **Mode**: 적용
    - **DistributionStatus**: 성공

적응형 범위의 성공을 확인했습니다.
